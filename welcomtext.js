const welcome = (number, groupname) => {
    return `Hola, @${number}. Bienvenido a ${groupname}`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adios pedazo de aborto @${number}. Nunca te quisimos 👋`
}
exports.bye = bye